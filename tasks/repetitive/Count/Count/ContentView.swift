//
//  ContentView.swift
//  Count
//
//  Created by Andrew Smith on 8/16/25.
//
let C4A_OUR_SETTINGS_FILE = "/opt/c4a/protected/ro/task_settings/lines"

import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Image(systemName: "globe")
                .imageScale(.large)
                .foregroundStyle(.tint)
            Text("Hello, world!")
        }
        .padding()
    }
}

#Preview {
    ContentView()
}
